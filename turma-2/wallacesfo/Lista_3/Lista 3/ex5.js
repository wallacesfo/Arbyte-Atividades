const rs = require('readline-sync')

let per = rs.question('Insira 0 para sair ou qualquer outra tecla para continuar ')

while(per!=0){
    let per = rs.question('Insira 0 para sair ou qualquer outra tecla para continuar ')
    if(per == 0){ break
    
    }

    else{
        
    }

}

